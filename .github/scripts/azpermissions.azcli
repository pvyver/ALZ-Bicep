# Create a service principal for a resource group using a preferred name and role
az ad sp create-for-rbac --name spn-azure-alz `
                         --role owner `
                         --scopes / `


az ad sp create-for-rbac --name "spn-azure-alz" --role owner `
    --scopes / `
    --sdk-auth